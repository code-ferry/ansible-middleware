- name: Clean log directory
  file:
    path: "{{ item }}"
    state: absent
  with_items:
  - "{{ clickhouse_data_dir }}"

- name: Create log directory
  file:
    name: "{{ item }}"
    state: directory
  with_items:
  - "{{ clickhouse_data_dir }}"
