{# 计算中backup master的服务地址, 生成字符串, 可用于输出到文件 #}
{% for host in groups['hbase'] %}
{% if hostvars[host].backup_master is defined and hostvars[host].backup_master == "yes" %}
{{ host }}
{% endif %}
{% endfor %}
