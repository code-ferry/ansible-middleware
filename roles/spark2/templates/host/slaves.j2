{# 计算中slave的服务地址 #}
{% set slaves = [] %}
{% for host in groups['spark2'] %}
{% if hostvars[host].slave is defined and hostvars[host].slave == "yes" %}
{{ slaves.append(host) -}}
{% endif %}
{% endfor %}
{{ slaves }}
